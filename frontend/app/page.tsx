import LoginPage from "./(public)/login/page";

/**
 * Página inicial da aplicação.
 * Atualmente renderiza a página de login por padrão.
 */
export default function Home() {
  return (
    <LoginPage />
  );
}
